soma(3, 5)

// function declaration
function soma(x, y) { // Poder ser chamada antes de ser declarada
    return x + y
}

// function expression
const sub = function (x, y) {
    return x - y
}

// named function expression
const mult = function mult(y, x) {
    return y * x
}